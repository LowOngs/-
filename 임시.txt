/* === Past Posts(블로그 보관함) fixed 16 rows, inner scroll, with border === */
/* ⬇ 필요 시 #BlogArchive1 → 실제 위젯 ID로 교체 */
#BlogArchive1 #ArchiveList{
  --row: 1.6em;
  height: calc(16 * var(--row));   /* 16줄 고정 높이 */
  overflow: auto;                  /* 내용만 스크롤 */
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  padding: 6px;
  box-sizing: border-box;
  background: #fff;
  overscroll-behavior: contain;
}

/* 리스트 라인 간격/여백 정리 */
#BlogArchive1 #ArchiveList,
#BlogArchive1 #ArchiveList ul,
#BlogArchive1 #ArchiveList li{
  line-height: var(--row);
  margin: 0;
  padding: 0;
}

/* 항목 줄바꿈 방지(월/제목 잘림 최소화) */
#BlogArchive1 #ArchiveList a{
  white-space: nowrap;
}

/* 스크롤바 미세 스타일(크롬/엣지/사파리) */
#BlogArchive1 #ArchiveList::-webkit-scrollbar{ width: 10px; }
#BlogArchive1 #ArchiveList::-webkit-scrollbar-thumb{
  background: #d1d5db; border-radius: 8px;
}
#BlogArchive1 #ArchiveList::-webkit-scrollbar-track{
  background: transparent;
}

/* ── 디버깅 모드(적용 여부 확인용) ──
#BlogArchive1 #ArchiveList{ outline: 2px dashed #22c55e; }
*/
